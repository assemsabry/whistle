# Inference

Whistle accepts football match video and produces anonymous, inspectable outputs.

Expected outputs include detections, anonymous track IDs, ball state, basic events, annotated video, and JSON/CSV/Parquet exports. The pipeline must not infer player names, positions, jersey numbers, or player ratings.

Released checkpoints are downloaded from Hugging Face; the inference code remains in this GitHub repository.
