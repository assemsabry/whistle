# Data and Provenance

Every training source must have a source URL, revision or date, checksum manifest, label mapping, preprocessing description, and redistribution status.

Raw videos and images are not committed to Git. When an upstream license does not allow redistribution, Whistle publishes a downloader and manifest instead of the raw files. See `docs/data_provenance.md` for the current audit.
