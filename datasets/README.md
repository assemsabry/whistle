# Dataset workspace

Large datasets are intentionally excluded from Git. Store local downloads under this directory when working from a documented manifest, and keep source URLs, revisions, checksums, labels, and license terms in `docs/data_provenance.md`.
